# پرامپت‌های آماده برای Claude Code — پیاده‌سازی فاز به فاز TechStore

**نحوه استفاده:**
1. فایل `PRD-فروشگاه-محصولات-کامپیوتری.md` را داخل ریشه پروژه (یا `docs/`) قرار دهید.
2. برای هر فاز، پرامپت مربوطه را عیناً کپی کرده و در Claude Code اجرا کنید — به ترتیب، بدون رد شدن از فازها.
3. بعد از هر فاز، پرامپت «تست و تایید» همان فاز را هم اجرا کنید تا قبل از رفتن سراغ فاز بعد، مطمئن شوید کار درست انجام شده.
4. اگر فاز جدید را در یک چت/session تازه شروع می‌کنید، حتماً از پرامپت «شروع Session جدید» در ابتدای این فایل استفاده کنید.

---

## 🔄 پرامپت شروع Session جدید (اگر چت قبلی را ادامه نمی‌دهید)

```
فایل PRD.md را در ریشه پروژه بخوان و کامل درک کن.
سپس ساختار فعلی پروژه (پوشه‌ها، مدل‌ها، migrationها، فایل settings) را بررسی کن
تا بفهمی تا کدام فاز از roadmap پیاده‌سازی شده است.
خلاصه‌ای از وضعیت فعلی پروژه (چه چیزی پیاده‌سازی شده، چه چیزی نشده) به من بده
و بعد از تایید من، فاز بعدی را شروع کن.
```

---

## فاز ۰ — راه‌اندازی پروژه

```
فایل PRD.md را بخوان.
فاز ۰ (راه‌اندازی پروژه) را دقیقاً طبق بخش «۲. معماری فنی» و «۵. فاز ۰» این سند پیاده‌سازی کن.

جزئیات مورد انتظار:
- پروژه Django 5.x را با ساختار apps پیشنهادی در PRD بساز
  (config, apps/accounts, apps/catalog, apps/inventory, apps/cart, apps/orders,
   apps/payments, apps/shipping, apps/reviews, apps/promotions,
   apps/notifications, apps/support)
- تنظیم settings به‌صورت جداشده برای base/dev/production (پوشه config/settings/)
- استفاده از django-environ برای خواندن متغیرهای محیطی از .env
- نصب و تنظیم Django REST Framework
- تنظیم اتصال به PostgreSQL (با مقادیر پیش‌فرض قابل override از .env)
- تنظیم django-cors-headers
- ساخت requirements.txt (یا pyproject.toml اگر ترجیح می‌دهی) با نسخه‌های پایدار
- ساخت .gitignore مناسب پروژه Django
- ساخت .env.example با تمام متغیرهای لازم (بدون مقدار واقعی حساس)
- یک اپ health-check ساده یا endpoint /api/health/ برای تست زنده بودن سرور

در پایان:
- دستورهای لازم برای اجرای پروژه (migrate, runserver) را به من بگو.
- مطمئن شو `python manage.py check` بدون خطا اجرا می‌شود.
```

**✅ تست و تایید فاز ۰:**
```
پروژه را با manage.py check بررسی کن، migrate اولیه را اجرا کن،
و با curl یا httpie نشان بده endpoint سلامت (health check) پاسخ ۲۰۰ می‌دهد.
```

---

## فاز ۱ — احراز هویت و کاربران (accounts)

```
طبق PRD.md، فاز ۱ (احراز هویت و کاربران) را پیاده‌سازی کن.
قبلش وضعیت فعلی پروژه را بررسی کن تا با ساختار فاز ۰ هماهنگ باشد.

جزئیات مورد انتظار:
- Custom User Model در apps/accounts طبق بخش ۳.۱ PRD
  (email به‌عنوان فیلد اصلی لاگین، phone_number، is_verified)
- مدل Address طبق PRD با ارتباط به User
- نصب و تنظیم djangorestframework-simplejwt
- Serializer و View برای:
  - ثبت‌نام (/api/auth/register/)
  - ورود و دریافت access/refresh token (/api/auth/login/)
  - رفرش توکن (/api/auth/refresh/)
  - مشاهده و ویرایش پروفایل (/api/auth/me/)
- CRUD کامل برای آدرس‌ها (/api/addresses/) با permission که هر کاربر فقط آدرس‌های خودش را ببیند
- ثبت مدل‌ها در Django Admin
- نوشتن تست واحد (pytest یا Django TestCase) برای: ثبت‌نام موفق، ثبت‌نام تکراری،
  لاگین موفق/ناموفق، دسترسی به آدرس فقط توسط مالک آن

در پایان خلاصه‌ای از endpointهای ساخته‌شده و نمونه request/response بده.
```

**✅ تست و تایید فاز ۱:**
```
تست‌های واحد فاز ۱ را اجرا کن و نتیجه را نشان بده.
سپس با curl یک کاربر تست ثبت‌نام کن، لاگین کن، توکن بگیر،
و با آن توکن یک آدرس اضافه کن تا مطمئن شویم auth کامل کار می‌کند.
```

---

## فاز ۲ — کاتالوگ محصولات (catalog)

```
طبق PRD.md، فاز ۲ (کاتالوگ محصولات) را پیاده‌سازی کن.

جزئیات مورد انتظار:
- مدل‌های Category (درختی با self FK)، Brand، Product، ProductImage،
  ProductAttribute، ProductAttributeValue، ProductVariant طبق بخش ۳.۲ PRD
- تولید خودکار slug برای Category و Product
- Serializer های جداگانه برای List (خلاصه) و Detail (کامل شامل تصاویر و attributeها)
- ViewSet با:
  - فیلتر بر اساس category, brand, min_price, max_price, و ویژگی‌های فنی
  - جستجوی متنی (search) روی title و description
  - مرتب‌سازی (ordering) بر اساس قیمت، جدیدترین، محبوب‌ترین
  - Pagination (page_size پیش‌فرض ۲۰ طبق الزامات PRD)
- ثبت کامل همه مدل‌ها در Django Admin با امکان مدیریت راحت
  (inline برای ProductImage و ProductAttributeValue داخل صفحه Product)
- تست واحد برای فیلتر، جستجو و صفحه‌بندی

در پایان چند محصول نمونه (seed data) با فیکسچر یا management command بساز
تا بتوانیم API را با داده واقعی تست کنیم.
```

**✅ تست و تایید فاز ۲:**
```
seed data را اجرا کن، سپس چند درخواست curl مختلف بزن:
لیست محصولات، فیلتر بر اساس brand، فیلتر بر اساس بازه قیمت، و جستجوی متنی.
نتایج را نشان بده و مطمئن شو pagination درست کار می‌کند.
```

---

## فاز ۳ — موجودی (inventory)

```
طبق PRD.md، فاز ۳ (موجودی) را پیاده‌سازی کن.

جزئیات مورد انتظار:
- مدل‌های Warehouse و StockItem طبق بخش ۳.۳ PRD
- اتصال StockItem به Product/ProductVariant
- متد یا سرویس برای رزرو موجودی هنگام افزودن به سبد (reserved_quantity)
  و آزادسازی آن در صورت انقضای سبد یا لغو سفارش
- به‌روزرسانی فیلد stock/quantity در پاسخ API محصول (فاز ۲) تا موجودی واقعی نمایش داده شود
- منطق atomic (با select_for_update یا F() expressions) برای جلوگیری از race condition
  و overselling هنگام کاهش موجودی
- خطای مناسب (409 یا 400 با پیام مشخص) وقتی موجودی کافی نیست
- تست واحد شامل تست همزمانی ساده (دو درخواست هم‌زمان روی آخرین موجودی)

در پایان توضیح بده منطق رزرو/آزادسازی موجودی دقیقاً چطور کار می‌کند.
```

**✅ تست و تایید فاز ۳:**
```
یک محصول با موجودی ۱ عدد بساز، سپس نشان بده که یک بار خرید موفق است
و درخواست دوم برای همان محصول خطای «موجودی کافی نیست» می‌دهد.
```

---

## فاز ۴ — سبد خرید (cart)

```
طبق PRD.md، فاز ۴ (سبد خرید) را پیاده‌سازی کن.

جزئیات مورد انتظار:
- مدل‌های Cart و CartItem طبق بخش ۳.۴ PRD
- پشتیبانی از سبد کاربر مهمان (بر اساس session_key) و سبد کاربر لاگین‌شده (بر اساس user)
- منطق merge: وقتی کاربر مهمان لاگین می‌کند، سبد session او با سبد کاربر (اگر وجود دارد) ترکیب شود
- APIهای:
  - GET /api/cart/  (مشاهده سبد فعلی)
  - POST /api/cart/items/  (افزودن آیتم)
  - PATCH /api/cart/items/{id}/  (تغییر تعداد)
  - DELETE /api/cart/items/{id}/  (حذف آیتم)
- در افزودن آیتم، موجودی از اپ inventory (فاز ۳) چک شود
- قیمت واحد در لحظه افزودن snapshot گرفته شود (unit_price_snapshot)
- تست واحد برای: افزودن به سبد مهمان، لاگین و merge شدن سبد، تلاش برای افزودن بیش از موجودی

در پایان یک سناریوی کامل نمونه (curl) نشان بده: سبد مهمان → لاگین → merge.
```

**✅ تست و تایید فاز ۴:**
```
سناریوی merge سبد را عملاً با curl اجرا کن (سبد بدون لاگین بساز،
سپس با همان session لاگین کن) و نشان بده آیتم‌ها حفظ شده‌اند.
```

---

## فاز ۵ — سفارش‌ها (orders)

```
طبق PRD.md، فاز ۵ (سفارش‌ها) را پیاده‌سازی کن.

جزئیات مورد انتظار:
- مدل‌های Order، OrderItem، OrderStatusHistory طبق بخش ۳.۵ PRD
- فرآیند checkout: تبدیل Cart فعلی کاربر به Order
  (کپی آیتم‌ها، قفل موجودی، محاسبه total_amount)
- محاسبه هزینه ارسال (فعلاً ساده، بر اساس ShippingMethod ثابت — جزئیات کامل در فاز مربوطه نیست)
- تولید order_number یکتا (مثلاً بر اساس تاریخ + شماره ترتیبی)
- API:
  - POST /api/orders/checkout/  (ساخت سفارش از سبد فعلی)
  - GET /api/orders/  (لیست سفارش‌های کاربر لاگین‌شده)
  - GET /api/orders/{id}/  (جزئیات یک سفارش، فقط برای مالک آن)
- هر تغییر وضعیت سفارش باید یک رکورد در OrderStatusHistory ثبت کند
- تست واحد برای: checkout موفق، تلاش کاربر دیگر برای دیدن سفارش کسی دیگر (باید ۴۰۳/۴۰۴ بدهد)

توجه: در این فاز هنوز پرداخت واقعی وصل نیست؛ سفارش با وضعیت pending ساخته می‌شود.
```

**✅ تست و تایید فاز ۵:**
```
یک سبد پر از آیتم بساز، checkout بزن، سفارش ساخته‌شده را با GET بگیر
و نشان بده OrderStatusHistory رکورد اولیه (pending) دارد.
```

---

## فاز ۶ — پرداخت (payments)

```
طبق PRD.md، فاز ۶ (پرداخت) را پیاده‌سازی کن.
درگاه پیش‌فرض را زرین‌پال (Sandbox) در نظر بگیر، اما ساختار را طوری بساز
که افزودن درگاه دوم (مثل آیدی‌پی یا Stripe) ساده باشد (الگوی Strategy/Adapter).

جزئیات مورد انتظار:
- مدل Payment طبق بخش ۳.۶ PRD
- سرویس انتزاعی PaymentGateway با پیاده‌سازی ZarinpalGateway (Sandbox)
- API:
  - POST /api/payments/initiate/  (ساخت تراکنش و گرفتن لینک پرداخت درگاه)
  - GET/POST /api/payments/callback/  (دریافت callback درگاه، verify تراکنش)
- بعد از verify موفق: وضعیت Payment و Order هر دو به‌روزرسانی شوند
  و رکورد OrderStatusHistory جدید (paid) ثبت شود
- در صورت پرداخت ناموفق: وضعیت مناسب ثبت شود و کاربر امکان تلاش مجدد داشته باشد
- کلیدهای API درگاه در .env و از طریق settings خوانده شود (هرگز hardcode نشود)
- تست واحد با mock کردن پاسخ درگاه (بدون تماس واقعی با زرین‌پال)

در پایان توضیح بده برای تست واقعی Sandbox چه مقادیری در .env لازم است.
```

**✅ تست و تایید فاز ۶:**
```
با mock کردن درگاه، یک تراکنش کامل initiate → callback موفق را تست کن
و نشان بده وضعیت سفارش از pending به paid تغییر کرده است.
همچنین سناریوی callback ناموفق را هم تست کن.
```

---

## فاز ۷ — تخفیف و کمپین (promotions)

```
طبق PRD.md، فاز ۷ (تخفیف و کمپین) را پیاده‌سازی کن.

جزئیات مورد انتظار:
- مدل Coupon طبق بخش ۳.۹ PRD
- منطق اعتبارسنجی کد تخفیف: فعال بودن، بازه تاریخ valid_from/valid_to،
  حداقل مبلغ سفارش، سقف تعداد استفاده (max_uses/used_count)
- API: POST /api/cart/apply-coupon/  (اعمال کد روی سبد فعلی)
- ذخیره discount_amount محاسبه‌شده روی Cart/Order هنگام checkout (فاز ۵)
- افزایش used_count فقط پس از پرداخت موفق (نه صرفاً اعمال روی سبد)، تا کد تخفیف
  توسط کاربرانی که پرداخت را کامل نمی‌کنند هدر نرود
- تست واحد برای: کد معتبر، کد منقضی، کد با حداقل خرید رعایت‌نشده، کد با سقف استفاده تمام‌شده

در پایان چند کد تخفیف نمونه با curl بساز و اعمال کن.
```

**✅ تست و تایید فاز ۷:**
```
یک کد تخفیف ۲۰٪ با حداقل خرید مشخص بساز، روی یک سبد کوچک‌تر از حداقل امتحان کن
(باید رد شود)، سپس روی سبد بزرگ‌تر اعمال کن و مبلغ نهایی صحیح را نشان بده.
```

---

## فاز ۸ — نظرات و امتیاز (reviews)

```
طبق PRD.md، فاز ۸ (نظرات و امتیاز) را پیاده‌سازی کن.

جزئیات مورد انتظار:
- مدل Review طبق بخش ۳.۸ PRD
- محدودیت: فقط کاربری که Order با status=delivered (یا حداقل paid، طبق تصمیم خودت
  و مستندش کن) شامل آن محصول دارد، اجازه ثبت نظر دارد
- هر کاربر فقط یک نظر برای هر محصول (unique_together)
- فیلد is_approved برای moderation دستی توسط ادمین قبل از نمایش عمومی
- محاسبه و کش کردن میانگین امتیاز (average_rating) روی مدل Product
  (یا با annotate در query، یا denormalized field به‌روزشونده با signal)
- API: GET/POST /api/products/{id}/reviews/
- تست واحد: کاربری که نخریده نمی‌تواند نظر بدهد، کاربری که خریده می‌تواند،
  نظر تکراری رد می‌شود

توضیح بده کدام رویکرد را برای محاسبه میانگین امتیاز انتخاب کردی و چرا.
```

**✅ تست و تایید فاز ۸:**
```
یک سفارش تحویل‌شده تست بساز، نشان بده کاربر آن سفارش می‌تواند نظر ثبت کند
و کاربر دیگری که این محصول را نخریده با خطای مناسب مواجه می‌شود.
```

---

## فاز ۹ — پشتیبانی (support)

```
طبق PRD.md، فاز ۹ (پشتیبانی) را پیاده‌سازی کن.

جزئیات مورد انتظار:
- مدل‌های Ticket و TicketMessage طبق بخش ۳.۱۰ PRD
- API:
  - POST /api/tickets/  (ایجاد تیکت جدید با پیام اول)
  - GET /api/tickets/  (لیست تیکت‌های کاربر لاگین‌شده)
  - GET /api/tickets/{id}/  (جزئیات + همه پیام‌ها، فقط برای مالک یا ادمین)
  - POST /api/tickets/{id}/messages/  (ارسال پیام جدید در تیکت)
- تغییر وضعیت تیکت (open/answered/closed) توسط ادمین از طریق Django Admin
  یا یک endpoint مخصوص staff
- permission: کاربر عادی فقط تیکت‌های خودش را می‌بیند
- تست واحد برای دسترسی و جریان کامل تیکت

در پایان یک سناریوی نمونه (ایجاد تیکت → پاسخ ادمین → بستن تیکت) با curl نشان بده.
```

**✅ تست و تایید فاز ۹:**
```
یک تیکت با کاربر تست بساز، یک پیام اضافه کن، از طریق Admin وضعیت را به
closed تغییر بده و نشان بده API وضعیت جدید را برمی‌گرداند.
```

---

## فاز ۱۰ — اعلان‌ها و Celery (notifications)

```
طبق PRD.md، فاز ۱۰ (اعلان‌ها و Celery) را پیاده‌سازی کن.

جزئیات مورد انتظار:
- نصب و تنظیم Celery با Redis به‌عنوان broker
- ساخت celery.py در config/ و اتصال به Django settings
- تسک async برای ارسال ایمیل تایید سفارش بعد از پرداخت موفق (فاز ۶)
- تسک async برای ارسال ایمیل تغییر وضعیت سفارش (مثلاً وقتی به shipped تغییر می‌کند)
- در محیط dev از django.core.mail.backends.console یا MailHog استفاده کن
  تا نیازی به SMTP واقعی نباشد
- تمپلیت ساده HTML/متنی برای ایمیل‌ها
- تست واحد که تسک به‌درستی صف می‌شود (با CELERY_TASK_ALWAYS_EAGER=True در تست)

در پایان دستور راه‌اندازی worker (celery -A config worker) را بگو.
```

**✅ تست و تایید فاز ۱۰:**
```
یک سفارش را به‌صورت تستی پرداخت‌شده کن و نشان بده ایمیل تایید سفارش
در کنسول (یا MailHog) ظاهر می‌شود.
```

---

## فاز ۱۱ — سخت‌سازی و آماده‌سازی Production

```
طبق PRD.md، فاز ۱۱ (سخت‌سازی و Production) را پیاده‌سازی کن.

جزئیات مورد انتظار:
- بازبینی همه ViewSet ها و اطمینان از permission_classes صحیح
  (IsAuthenticated جایی که لازم است، IsOwner برای منابع شخصی، IsAdminUser برای مدیریت)
- افزودن throttling (Rate limiting) روی endpointهای حساس مثل login و initiate payment
  (django-ratelimit یا DRF throttle classes)
- تنظیم لاگ‌گیری ساخت‌یافته (logging config در settings) با فایل جدا برای خطاهای
  پرداخت و سفارش
- تنظیم نهایی CORS_ALLOWED_ORIGINS و ALLOWED_HOSTS برای production
- تنظیم static/media برای production با پشتیبانی S3-compatible (django-storages)
- نوشتن Dockerfile برای اپ Django (multi-stage build اگر مناسب است)
- نوشتن docker-compose.yml شامل سرویس‌های: web (Django+gunicorn)، db (postgres)،
  redis، celery worker
- بررسی امنیتی پایه: DEBUG=False در production، SECRET_KEY از env، بررسی IDOR
  در endpointهای سفارش/پرداخت/آدرس (هر کاربر فقط منابع خودش)

در پایان چک‌لیستی از موارد امنیتی که پوشش دادی بده.
```

**✅ تست و تایید فاز ۱۱:**
```
docker-compose up را اجرا کن و نشان بده همه سرویس‌ها (web, db, redis, celery)
بالا می‌آیند و health check همچنان پاسخ می‌دهد.
```

---

## فاز ۱۲ — مستندسازی API

```
طبق PRD.md، فاز ۱۲ (مستندسازی API) را پیاده‌سازی کن.

جزئیات مورد انتظار:
- نصب و تنظیم drf-spectacular
- افزودن endpoint /api/schema/ و رابط Swagger UI در /api/docs/
- افزودن description و example مناسب برای مهم‌ترین endpointها
  (auth, catalog, cart, orders, payments) با استفاده از @extend_schema
- گروه‌بندی endpointها بر اساس tag (accounts, catalog, cart, orders, payments, ...)
- بررسی نهایی که همه endpointهای فازهای قبل در Swagger قابل مشاهده و تست هستند

در پایان لینک /api/docs/ را به من بده و لیست کامل tagهای موجود در مستندات را نشان بده.
```

**✅ تست و تایید فاز ۱۲:**
```
صفحه /api/docs/ را باز کن (یا خروجی JSON شمای آن را نشان بده)
و تایید کن تمام APIهای فازهای ۱ تا ۱۱ در آن دیده می‌شوند.
```

---

## 📌 نکات کلی هنگام اجرای پرامپت‌ها

- اگر Claude Code در میانه یک فاز کاری غیرمنتظره انجام داد یا از مسیر PRD منحرف شد،
  بگویید: **"به PRD.md برگرد و دقیقاً طبق فاز فعلی پیش برو."**
- بعد از هر فاز، `git commit` جداگانه بگیرید (می‌توانید از Claude Code هم بخواهید این کار را انجام دهد)
  تا در صورت نیاز به rollback، فازها از هم جدا باشند.
- اگر فاز خاصی خیلی بزرگ به نظر رسید، می‌توانید آن را به زیرفازها بشکنید
  (مثلاً فاز ۲ را به «مدل‌ها» و «APIها» جدا کنید) — این تصمیم را می‌توانید حین کار بگیرید.
